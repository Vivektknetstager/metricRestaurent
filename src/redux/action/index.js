export const increase=()=>({
    type: 'INCREASE'
  });
  export const decrease=()=>({
    type: 'DECREASE'
  });
  export const startLoader=()=>({
    type: 'START_LOADER'
  });
  export const stopLoader=()=>({
    type: 'STOP_LOADER'
  });